<div>
    <div class="form-group col-12">


        <textarea id="{{ $name }}" name="{{ $name }}" class="form-control " {{ $variables }}
            value="{{ old($name) }}"></textarea>

    </div>
</div>
